<UserControl x:Class="MiniPlayerDeskBand.PlayerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="200"
             Background="Transparent">
    
    <UserControl.Resources>
        <Style x:Key="MediaBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#11FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/> <!-- Icon -->
            <ColumnDefinition Width="*"/>    <!-- Text -->
            <ColumnDefinition Width="Auto"/> <!-- Buttons -->
        </Grid.ColumnDefinitions>

        <Image x:Name="AppIcon" Grid.Column="0" Width="16" Height="16" Margin="5,0,5,0" RenderOptions.BitmapScalingMode="HighQuality"/>

        <TextBlock x:Name="TrackInfo" Grid.Column="1" 
                   VerticalAlignment="Center" 
                   Foreground="White" 
                   FontSize="10"
                   TextTrimming="CharacterEllipsis"
                   Text="Ready"
                   Margin="0,0,5,0"/>

        <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="0,0,5,0">
            <Button Style="{StaticResource MediaBtn}" Content="&#xE892;" Click="Prev_Click"/>
            <Button x:Name="BtnPlayPause" Style="{StaticResource MediaBtn}" Content="&#xE768;" Click="PlayPause_Click"/>
            <Button Style="{StaticResource MediaBtn}" Content="&#xE893;" Click="Next_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
