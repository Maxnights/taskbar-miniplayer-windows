<Window x:Class="MiniPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MiniPlayer" 
        SizeToContent="WidthAndHeight"
        MinWidth="100" MinHeight="32"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        Topmost="True"
        ShowInTaskbar="False"
        Loaded="Window_Loaded"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <Style x:Key="MediaBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Window}}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#11888888"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#22888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="#F21E1E1E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#22FFFFFF"/>
            <Setter Property="Grid.IsSharedSizeScope" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12"
                                SnapsToDevicePixels="True">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="20" Direction="270" Opacity="0.4" ShadowDepth="3"/>
                            </Border.Effect>
                            <ScrollViewer Margin="0" Background="Transparent">
                                <ItemsPresenter Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="Bg" Background="Transparent" CornerRadius="6" Margin="2,1" SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="24" SharedSizeGroup="CheckCol"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto" SharedSizeGroup="ArrowCol"/>
                                </Grid.ColumnDefinitions>
                                <Path x:Name="CheckMark" Grid.Column="0" Visibility="Collapsed" Data="M0,5 L3,8 L8,0" Stroke="White" StrokeThickness="1.5" HorizontalAlignment="Center" VerticalAlignment="Center" SnapsToDevicePixels="True"/>
                                <ContentPresenter Grid.Column="1" ContentSource="Header" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" VerticalAlignment="Center"/>
                                <Path x:Name="RightArrow" Grid.Column="2" Visibility="Collapsed" Data="M0,0 L4,4 L0,8" Stroke="#AAFFFFFF" StrokeThickness="1" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5,0,5,0"/>
                                <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False" IsOpen="{Binding IsSubmenuOpen, RelativeSource={RelativeSource TemplatedParent}}" Placement="Right" PopupAnimation="Fade">
                                    <Border Background="#F21E1E1E" BorderBrush="#22FFFFFF" BorderThickness="1" CornerRadius="12" Padding="5">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="15" Opacity="0.3"/>
                                        </Border.Effect>
                                        <ScrollViewer Background="Transparent">
                                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="True">
                                <Setter TargetName="RightArrow" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#66FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="#1AFFFFFF"/>
            <Setter Property="Margin" Value="6,4,6,4"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Rectangle Height="1" Fill="{TemplateBinding Background}" SnapsToDevicePixels="True"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="Transparent" Padding="6" MouseWheel="TrackInfo_MouseWheel">
        <Border.ContextMenu>
            <ContextMenu x:Name="SourceMenu" Opened="SourceMenu_Opened" Closed="SourceMenu_Closed" BorderThickness="0">
            </ContextMenu>
        </Border.ContextMenu>

        <Grid x:Name="MainGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="Col0" Width="Auto"/>
                <ColumnDefinition x:Name="Col1" Width="Auto"/> 
                <ColumnDefinition x:Name="Col2" Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition x:Name="Row0" Height="Auto"/>
                <RowDefinition x:Name="Row1" Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid x:Name="IconContainer" Grid.Column="0" Grid.RowSpan="2" Margin="4,0,8,0" VerticalAlignment="Center">
                <Image x:Name="AppIcon" Width="18" Height="18" RenderOptions.BitmapScalingMode="HighQuality"/>
                <Image x:Name="ThumbnailImage" Width="24" Height="24" Stretch="UniformToFill" RenderOptions.BitmapScalingMode="HighQuality">
                    <Image.Clip>
                        <RectangleGeometry RadiusX="4" RadiusY="4" Rect="0,0,24,24"/>
                    </Image.Clip>
                </Image>
            </Grid>

            <StackPanel x:Name="ControlsPanel" Grid.Column="1" Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                <Button Style="{StaticResource MediaBtn}" Content="&#xE892;" Click="Prev_Click"/>
                <Button x:Name="BtnPlayPause" Style="{StaticResource MediaBtn}" Content="&#xE768;" Click="PlayPause_Click" Margin="2,0"/>
                <Button Style="{StaticResource MediaBtn}" Content="&#xE893;" Click="Next_Click"/>
            </StackPanel>

            <TextBlock x:Name="TrackInfo" 
                       VerticalAlignment="Center" 
                       FontSize="11"
                       TextTrimming="CharacterEllipsis"
                       Text="Ready"
                       Margin="0"/>
        </Grid>
    </Border>
</Window>
